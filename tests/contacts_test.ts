import { configData } from "../config/config-data";
const randomEmail = require('random-email');
Feature('Contacts API');

Scenario('CRUD Contacts', async ({ I, usersApi }) => {

}).tag('users-api-test-2');